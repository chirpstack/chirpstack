apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: chirpstack
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/instance: chirpstack
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
