apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: chirpstack
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: chirpstack
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
